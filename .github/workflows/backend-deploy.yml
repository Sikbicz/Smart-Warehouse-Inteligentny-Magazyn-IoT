# backend-deploy.yml

name: Build and Deploy .NET Backend

on:
  push:
    branches:
      - main
    paths:
      - 'InteligentnyMagazyn/**'
      - '.github/workflows/backend-deploy.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 8.0
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0'

      - name: Build and publish .NET app
        run: |
          dotnet restore InteligentnyMagazyn/InteligentnyMagazyn.Api/InteligentnyMagazyn.Api.csproj
          dotnet build InteligentnyMagazyn/InteligentnyMagazyn.Api/InteligentnyMagazyn.Api.csproj --configuration Release --no-restore
          dotnet publish InteligentnyMagazyn/InteligentnyMagazyn.Api/InteligentnyMagazyn.Api.csproj -c Release -o ./publish --no-build

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          # Nazwa Twojej aplikacji w Azure
          app-name: 'sw-iot-2506-backend-api'
          # Sekret z profilem publikacji
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          # Folder, z którego wdrażamy
          package: './publish'
